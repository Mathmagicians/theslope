{
  // Generated by `npx nitro prepare --dir workers/sender` (part of `npm run ts:workers`).
  // Runtime + binding types come from worker-configuration.d.ts (`make typegen`), generated from this
  // worker's compatibility_date and flags — no @cloudflare/workers-types package entry.
  "extends": "./.nitro/types/tsconfig.json",
  // The Nitro base includes workers/sender/**, which would pull in the built bundle
  "exclude": ["node_modules", ".output"]
}
