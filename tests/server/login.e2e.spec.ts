import { describe, it, expect, vi } from 'vitest'

describe('test /auth/login endpoint', () => {
    it.todo( 'should return a 400 error when no credentials are passed', () => {
    })

    it.todo( 'should return a 400 error when a wrong body is passed', () => {
    })

    it.todo( 'should return a 404 error when incorrect credentials are passed', () => {
    })

    it.todo( 'should return user session with a heynabo tokoen when correct credentials are passed', () => {
    })
})
